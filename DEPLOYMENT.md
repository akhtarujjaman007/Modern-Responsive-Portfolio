# Deployment

## GitHub Pages

1. Push the repository to GitHub.
2. Run `npm install`.
3. Run `npm run build`.
4. Deploy the `dist` folder using your preferred GitHub Pages workflow.

## Vercel

Import the repository into Vercel. Use:

- Build command: `npm run build`
- Output directory: `dist`

Before deployment, replace the placeholder CV and contact details.
